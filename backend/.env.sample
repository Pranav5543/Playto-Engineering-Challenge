# Django Backend Environment Variables
# Copy this file to .env and update the values

# Django Secret Key (CHANGE THIS IN PRODUCTION!)
SECRET_KEY=django-insecure-*b=z51%9a9%4$nn&^h4pa)m-(pset#(!!rzm9@ca6qtd57y2)^

# Debug Mode (Set to False in production)
DEBUG=True

# Allowed Hosts (comma-separated, e.g., localhost,127.0.0.1,yourdomain.com)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# For SQLite (default for local development):
DATABASE_URL=sqlite:///db.sqlite3

# For PostgreSQL (production/Docker):
# DATABASE_URL=postgres://user:pass@db:5432/community_db

# CORS Settings (for frontend)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
